pipeline:
  build:
    image: ruby:2.0.0
    environment:
      - RAILS_ENV=test
    commands:
      - sleep 5
      - bundle install
      - bundle exec rspec spec
services:
  database:
    image: mysql
  redis:
    image: redis