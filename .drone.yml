pipeline:
  build:
    image: ruby2.0.0
    env:
      - RAILS_ENV=test
    commands:
      - sleep 5
      - bundle install
      - bundle exec rspec spec
services:
  database:
    image: mysql
  redis:
    image: redis